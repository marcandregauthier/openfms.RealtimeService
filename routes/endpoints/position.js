import Repository from '../../repository/position';


const receive = async (request, response) => {
    console.log('position.receive: ' + request.body);

    // Find LastState

    // Update

    
};


export default { receive };